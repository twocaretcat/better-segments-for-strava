name: 🛠️ Setup
description: Set up Bun and install dependencies.
runs:
  using: composite
  steps:
    - name: 🥖 Setup Bun
      uses: oven-sh/setup-bun@v1

    - name: 📥 Install dependencies
      shell: bash
      run: bun install
